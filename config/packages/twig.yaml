twig:
    default_path: '%kernel.project_dir%/templates'
    globals:
        welcome:
            title: '%app.title%'
            locales: '%app.locales%'

when@test:
    twig:
        strict_variables: true
